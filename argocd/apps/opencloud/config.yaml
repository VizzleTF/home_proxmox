# Upstream community chart (github.com/opencloud-eu/helm) is archived — no releases,
# no chart repo, no OCI package. Deployed as a plain workload via homelab-common.apps.
homelabOnly: true
wave: "2"
